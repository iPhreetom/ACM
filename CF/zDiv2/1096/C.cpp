#include<bits/stdc++.h>
#define int long long
#define double long double
using namespace std;

int ans[181];

void solve (int node) {
	try_angle();

}

signed main () {
	ios::sync_with_stdio(false),cin.tie(0),cout.tie(0);
	for (int i=1; i<=180; i++) {
		ans[i] = 1e10;d
	}
	for (int i=3; i<=180; i++) {
		solve(i);
	}
	return 0;
}
