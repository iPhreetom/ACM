// fyt
#include<bits/stdc++.h>
#include<ext/rope>

#define int long long
#define double long double
#define endl '\n'
using namespace std;
using namespace __gnu_cxx;


signed main(){
	ios::sync_with_stdio(false),cin.tie(0),cout.tie(0);
	rope<int> a(n,a,x);
	rope.replace(y,k,b.begin());
	return 0;
}
