// fyt
#include<bits/stdc++.h>
#define int long long
#define double long double
using namespace std;

int c,s;

signed main(){
	ios::sync_with_stdio(false),cin.tie(0),cout.tie(0);
	while(cin>>c>>s){
		if(c == 0 && s == 0)break;
		
	}
	return 0;
}
