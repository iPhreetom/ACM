// review the inverse module
// give a and m, if ax = 1(mod m), the x is the inverse mod of a (mod m)
	// if m is a prime number
	// we can use the Fermat's small theorem
	// the inverse mod is equal to a^(m-2) (mod m)
		// it can be deduced by a^(m-1) = 1 (mod m)
		// and a^m = a (mod m)
